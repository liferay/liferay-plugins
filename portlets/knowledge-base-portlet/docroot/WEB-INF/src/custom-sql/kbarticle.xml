<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.kb.knowledgebase.service.persistence.KBArticleFinder.countByGroupIds">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				KB_KBArticle
			WHERE
				[$GROUP_ID$] AND
				(head = [$TRUE$]) AND
				(template = [$FALSE$]) AND
				(draft = [$FALSE$])
		]]>
	</sql>
	<sql id="com.liferay.kb.knowledgebase.service.persistence.KBArticleFinder.countByS_U_G">
		<![CDATA[
			SELECT
				COUNT(DISTINCT KB_KBArticle.resourcePrimKey) AS COUNT_VALUE
			FROM
				KB_KBArticle
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = KB_KBArticle.resourcePrimKey)
			WHERE
				(KB_KBArticle.groupId = ?) AND
				(KB_KBArticle.head = [$TRUE$]) AND
				(Subscription.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.kb.knowledgebase.service.persistence.KBArticleFinder.countByG_P_T_Or_G_P_T_U">
		<![CDATA[
			SELECT
				COUNT(DISTINCT KB_KBArticle.resourcePrimKey) AS COUNT_VALUE
			FROM
				KB_KBArticle
			WHERE
				(groupId = ?) AND
				(parentResourcePrimKey = ?) AND
				(template = ?) AND
				(head = [$TRUE$]) AND
				(
					(draft = [$FALSE$]) OR
					(userId = ?)
				)
		]]>
	</sql>
	<sql id="com.liferay.kb.knowledgebase.service.persistence.KBArticleFinder.countByG_T_Or_G_T_U">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				KB_KBArticle
			WHERE
				(groupId = ?) AND
				(parentResourcePrimKey = ?) AND
				(template = ?) AND
				(head = [$TRUE$]) AND
				(
					(draft = [$FALSE$]) OR
					(userId = ?)
				)
		]]>
	</sql>
	<sql id="com.liferay.kb.knowledgebase.service.persistence.KBArticleFinder.findByGroupIds">
		<![CDATA[
			SELECT
				{KB_KBArticle.*}
			FROM
				KB_KBArticle
			WHERE
				[$GROUP_ID$] AND
				(head = [$TRUE$]) AND
				(template = [$FALSE$]) AND
				(draft = [$FALSE$])
			ORDER BY
				modifiedDate DESC
		]]>
	</sql>
	<sql id="com.liferay.kb.knowledgebase.service.persistence.KBArticleFinder.findByS_U_G">
		<![CDATA[
			SELECT
				DISTINCT {KB_KBArticle.*}
			FROM
				KB_KBArticle
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = KB_KBArticle.resourcePrimKey)
			WHERE
				(KB_KBArticle.groupId = ?) AND
				(KB_KBArticle.head = [$TRUE$]) AND
				(Subscription.userId = ?)
			ORDER BY
				title ASC,
				version ASC
		]]>
	</sql>
	<sql id="com.liferay.kb.knowledgebase.service.persistence.KBArticleFinder.findByG_P_T_Or_G_P_T_U">
		<![CDATA[
			SELECT
				{KB_KBArticle.*}
			FROM
				KB_KBArticle
			WHERE
				(groupId = ?) AND
				(parentResourcePrimKey = ?) AND
				(template = ?) AND
				(head = [$TRUE$]) AND
				(
					(draft = [$FALSE$]) OR
					(userId = ?)
				)
			ORDER BY
				title ASC,
				version ASC
		]]>
	</sql>
</custom-sql>