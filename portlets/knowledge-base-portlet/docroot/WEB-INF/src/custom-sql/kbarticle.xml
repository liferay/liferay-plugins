<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.knowledgebase.service.persistence.KBArticleFinder.countByU_G_H_T_D">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				KB_KBArticle
			WHERE
				(groupId = ?) AND
				(head = ?) AND
				(template = ?) AND
				(
					(userId = ?) OR
					(draft = ?)
				)
		]]>
	</sql>
	<sql id="com.liferay.knowledgebase.service.persistence.KBArticleFinder.countByU_R_H_D">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				KB_KBArticle
			WHERE
				(resourcePrimKey = ?) AND
				(head = ?) AND
				(
					(userId = ?) OR
					(draft = ?)
				)
		]]>
	</sql>
	<sql id="com.liferay.knowledgebase.service.persistence.KBArticleFinder.findByU_G_H_T_D">
		<![CDATA[
			SELECT
				{KB_KBArticle.*}
			FROM
				KB_KBArticle
			WHERE
				(groupId = ?) AND
				(head = ?) AND
				(template = ?) AND
				(
					(userId = ?) OR
					(draft = ?)
				)
			ORDER BY
				title ASC,
				version ASC
		]]>
	</sql>
	<sql id="com.liferay.knowledgebase.service.persistence.KBArticleFinder.findByU_R_H_D">
		<![CDATA[
			SELECT
				{KB_KBArticle.*}
			FROM
				KB_KBArticle
			WHERE
				(resourcePrimKey = ?) AND
				(head = ?) AND
				(
					(userId = ?) OR
					(draft = ?)
				)
			ORDER BY
				title ASC,
				version ASC
		]]>
	</sql>
</custom-sql>