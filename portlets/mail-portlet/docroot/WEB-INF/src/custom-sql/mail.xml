<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.mail.service.persistence.MessageFinder.countUnreadByAccountId">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				Mail_Message
			WHERE
				(Mail_Message.accountId = ?) AND NOT
				(Mail_Message.flags LIKE '%6,%')
		]]>
	</sql>
	<sql id="com.liferay.mail.service.persistence.MessageFinder.countUnreadByFolderId">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				Mail_Message
			WHERE
				(Mail_Message.folderId = ?) AND NOT
				(Mail_Message.flags LIKE '%6,%')
		]]>
	</sql>
</custom-sql>