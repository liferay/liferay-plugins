##
## DO NOT EDIT THIS FILE.
##
## To update the properties of this file, create a separate properties file
## named "build.${user.name}.properties" with the properties to overwrite.
##

##
## Application Server
##

    #
    # The default settings are for Tomcat 6.0.x. Additional settings are left
    # commented out as examples.
    #

    #
    # Specify the paths to an unzipped Glassfish v2 bundle.
    #
    #app.server.dir=${project.dir}/../bundles/glassfish
    #app.server.classes.portal.dir=${app.server.portal.dir}/WEB-INF/classes
    #app.server.lib.global.dir=${app.server.dir}/domains/domain1/lib
    #app.server.lib.portal.dir=${app.server.portal.dir}/WEB-INF/lib
    #app.server.portal.dir=${app.server.dir}/domains/domain1/applications/j2ee-modules/liferay-portal

    #
    # Specify the paths to an unzipped Glassfish v3 bundle.
    #
    #app.server.dir=${project.dir}/../bundles/glassfish
    #app.server.classes.portal.dir=${app.server.portal.dir}/WEB-INF/classes
    #app.server.lib.global.dir=${app.server.dir}/domains/domain1/lib
    #app.server.lib.portal.dir=${app.server.portal.dir}/WEB-INF/lib
    #app.server.portal.dir=${app.server.dir}/domains/domain1/applications/liferay-portal

    #
    # Specify the paths to an unzipped JBoss+Tomcat 4.2.x bundle.
    #
    #app.server.dir=${project.dir}/../bundles/jboss-tomcat-4.2.3
    #app.server.classes.portal.dir=${app.server.portal.dir}/WEB-INF/classes
    #app.server.lib.global.dir=${app.server.dir}/server/default/lib/ext
    #app.server.lib.portal.dir=${app.server.portal.dir}/WEB-INF/lib
    #app.server.portal.dir=${app.server.dir}/server/default/deploy/ROOT.war
    #app.server.zip.name=liferay-portal-jboss-tomcat-4.2-${lp.version}.zip

    #
    # Specify the paths to an unzipped JBoss+Tomcat 5.0.x bundle.
    #
    #app.server.dir=${project.dir}/../bundles/jboss-tomcat-5.0.0
    #app.server.classes.portal.dir=${app.server.portal.dir}/WEB-INF/classes
    #app.server.lib.global.dir=${app.server.dir}/server/default/lib/ext
    #app.server.lib.portal.dir=${app.server.portal.dir}/WEB-INF/lib
    #app.server.portal.dir=${app.server.dir}/server/default/deploy/ROOT.war
    #app.server.zip.name=liferay-portal-jboss-tomcat-5.0-${lp.version}.zip

    #
    # Specify the paths to an unzipped Tomcat 5.5.x bundle.
    #
    #app.server.dir=${project.dir}/../bundles/tomcat-5.5.27
    #app.server.classes.portal.dir=${app.server.portal.dir}/WEB-INF/classes
    #app.server.lib.global.dir=${app.server.dir}/common/lib/ext
    #app.server.lib.portal.dir=${app.server.portal.dir}/WEB-INF/lib
    #app.server.portal.dir=${app.server.dir}/webapps/ROOT
    #app.server.zip.name=liferay-portal-tomcat-5.5-${lp.version}.zip

    #
    # Specify the paths to an unzipped Tomcat 6.0.x bundle.
    #
    app.server.dir=${project.dir}/../bundles/tomcat-6.0.18
    app.server.classes.portal.dir=${app.server.portal.dir}/WEB-INF/classes
    app.server.lib.global.dir=${app.server.dir}/lib/ext
    app.server.lib.portal.dir=${app.server.portal.dir}/WEB-INF/lib
    app.server.portal.dir=${app.server.dir}/webapps/ROOT
    app.server.zip.name=liferay-portal-tomcat-6.0-${lp.version}.zip

##
## Auto Deploy
##

    #
    # Plugins will be deployed to this directory. Make sure that Liferay is
    # also set to scan this directory.
    #
    auto.deploy.dir=${app.server.dir}/../deploy

##
## Compiler
##

    ant.build.javac.source=1.5
    ant.build.javac.target=1.5

    javac.compiler=modern
    #javac.compiler=org.eclipse.jdt.core.JDTCompilerAdapter

    javac.debug=on
    javac.deprecation=off
    javac.fork=true
    javac.memoryMaximumSize=256m
    javac.nowarn=on

##
## Extension Environment
##

    #ext.work.dir=

##
## Libraries
##

    required.portal.jars=commons-logging.jar,log4j.jar,util-bridges.jar,util-java.jar,util-taglib.jar

##
## Operating Systems
##

    os.apple=Mac OS X
    os.unix=Linux,FreeBSD,Solaris,SunOS
    os.windows=Windows 95,Windows 98,Windows NT,Windows 2000,Windows 2003,Windows XP,Windows Vista

##
## Plugins
##

    #
    # Set the excludes and includes attributes. These attributes are read when
    # you run "ant" from the layouttpl, portlets, or themes directory. By
    # default, the ant task will go through every subdirectory. Setting these
    # attributes can limit it to just the plugins you're working on.
    #
    plugins.excludes=
    plugins.includes=*

##
## SVN
##

    svn.enabled=false

##
## Version
##

    #
    # Set the version of Liferay Portal that these plugins are designed for.
    #
    lp.version=6.0.0